export enum BirdSettings {
  birdSize = 40,
  birdRadius = birdSize / 2,
  jumpHeight = 80,
  gravity = 5,
  startPosition = 75,
  hitbox = 90,
}

export enum ObstacleSettings {
  gap = 250,
  minHeight = 100,
  movementSpeed = 3,
  width = 75,
}
